//Data persistency
//This code will add a user object to the database with the
//properties: uid, favorites, zip
var userData = {
	 
}



$(document).ready(function() {
	console.log('this is the current user', sessionStorage.user);
	
})